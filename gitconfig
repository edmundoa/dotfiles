[user]
  name = Edmundo Alvarez
  email = edmundo.alvarez@xing.com
[branch]
  autosetuprebase = always
[pull]
  rebase = true
[push]
  default = tracking
[color]
  status = auto
  branch = auto
  ui = auto
  interactive = auto
[alias]
  stauts = !sh -c 'gti status'
  satus = !sh -c 'gti status'
  aa = add --all
  dc = diff --cached
  lc = !sh -c 'CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD) && git log $1 $CURRENT_BRANCH ^origin/$CURRENT_BRANCH' -
[grep]
  lineNumber = true
